#!/usr/bin/env python3
"""
Jarvis CLI launcher.

This wrapper should behave like the installed `jarvis` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from jarvis_cli.main import main
    main()
